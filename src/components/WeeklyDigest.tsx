// Retired: the weekly digest is now rendered inline on the HQ via <WeekRecap />
// instead of as a modal. This file is kept as an empty stub so any lingering
// imports fail loudly — but no component in the app references it anymore.
export {};
